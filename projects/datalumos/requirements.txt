# Install with:
#   pip install -r requirements.txt
#
# Then install Playwright's Chromium browser with:
#   python -m playwright install chromium

pandas
requests
beautifulsoup4
rapidfuzz
playwright